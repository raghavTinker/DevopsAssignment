dist: focal
group: edge
stages:
- name: Linting
  if: branch = main AND type = push AND fork = false
jobs:
  include:
  - stage: Linting
    name: Linting app.py
    install:
    - pip3 install -r requirements.txt
    script:
    - pytest --flake8
